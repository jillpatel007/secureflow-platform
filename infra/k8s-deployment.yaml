apiVersion: apps/v1
kind: Deployment
metadata:
  name: secureflow-api
  namespace: secureflow
  annotations:
    # checkov:skip=CKV_K8S_43: local kind demo builds images locally; digest pinning applies to registry-pulled images in production
  labels:
    app: secureflow-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: secureflow-api
  template:
    metadata:
      labels:
        app: secureflow-api
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
      containers:
        - name: secureflow-api
          image: secureflow-api:0.1.0
          ports:
            - containerPort: 8000
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: ["ALL"]
