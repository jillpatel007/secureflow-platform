name: CI Security Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  secret-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Gitleaks secret scan
        run: |
          curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.21.2/gitleaks_8.21.2_linux_x64.tar.gz -o gitleaks.tar.gz
          tar -xzf gitleaks.tar.gz gitleaks
          ./gitleaks detect --source . --no-git --redact --verbose --exit-code 1 \
            --report-format sarif --report-path results.sarif

  sast:
    runs-on: ubuntu-latest
    container:
      image: semgrep/semgrep
    steps:
      - uses: actions/checkout@v4
      - name: Semgrep SAST scan
        run: semgrep scan --config=auto --error app/

  sca-and-iac:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Trivy dependency scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: fs
          scan-ref: app/api/
          scanners: vuln
      - name: Checkov IaC scan
        uses: bridgecrewio/checkov-action@master
        with:
          directory: infra/
          soft_fail: true

  build-and-scan-image:
    runs-on: ubuntu-latest
    needs: [secret-scan, sast]
    steps:
      - uses: actions/checkout@v4
      - name: Build image
        run: docker build -t secureflow-api:0.1.0 ./app/api
      - name: Trivy image scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: image
          image-ref: secureflow-api:0.1.0
      - name: Generate SBOM
        uses: anchore/sbom-action@v0
        with:
          image: secureflow-api:0.1.0
          format: spdx-json
